// const assert = require('chai').assert;

// const { getIsValidUser } = require('../RefactoringAPI/updatedQueryModifier');

// describe('Validating user credentials', () => {

//     it('it should return false if user credentials are empty', () => {
//         assert.equal(getIsValidUser(''), false)
//     });

//     it('it should return true if user credentials are valid', () => {
//         assert.equal(getIsValidUser('Don'), true)
//     });

//     it('it should return false if user credentials are in-valid', () => {
//         assert.equal(getIsValidUser('Pavan'), false)
//     });

// });